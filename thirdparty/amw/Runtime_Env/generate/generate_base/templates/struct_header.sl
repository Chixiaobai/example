#include <BoundedStream/BoundedStream.h>
/*
 Generated by the generate_base script.  DO NOT EDIT!
*/

#pragma pack(1) // Specify the alignment method as tight alignment
struct  ${struct_name}_struct;
typedef struct ${struct_name}_struct ${typedef_name};
struct  ${struct_name}_struct {
${fields}

${struct_name}_struct(){
${init_code}
}

~${struct_name}_struct(){
${finit_code}
}

void serialize(CAbstractStream & reqStream) const {
${serialize_code}
}

void deserialize(CAbstractStream & respStream){
${deserialization_code}
}

void deserializeFromArray(const void* data, size_t size){
    CBoundedStream  respStream;
    respStream.writeObject(data,size);
    deserialize(respStream);
}

void serializeToArray(void*& data, size_t& size)const{
    CBoundedStream  reqStream;
    serialize(reqStream);
    int len=(int)reqStream.getLength();
    char * temp=new char[len];
    reqStream.readObject(temp,len);
    data=temp;
    size=len;
}

};



#pragma pack() // Restore default alignment
