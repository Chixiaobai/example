/*This file is auto generated by RPCAutoCode. Please do not edit it.*/
#pragma once

#include "Base/IBase.h"
#include "DataType/SafeArray.h"

#define UNIQUE_FRAMEWORK_NAME "Framework"

#define FRAMEWORK_INTERFACE_ID 0

interface IFramework : public IBase {
    /*This function is used for change the maximal log level of system.
    only log which level is less than uiMaxLogLevel can be output.
    uiMaxLogLevel can be the following value:1-ERROR; 1-INFO; 2-WARNING; 3-DEBUG.*/
    virtual bool setLogLevel(/*[IN]*/ unsigned short uiMaxLogLevel) = 0;
    /*This function is used for set the dynamic property for hardware instance.
    Each hardware has it's own properties.You can get information at hardware's introduction.*/
    virtual bool setHardwareProperty(/*[IN]*/ const char* strAbsoluteHardwareName, /*[IN]*/ const char* strPropertyName,
                                     /*[IN]*/ const char* strPropertyValue) = 0;
    /*This function is used for enum all modules's version information.
    The name for these modules are stored in strModuleList.
    The version for these modules are stored in strModuleVersionList.*/
    virtual void getAllModuleVersions(/*[IN]*/ int intVersionType, /*OUT[string]*/ safearray_handle** strModuleList,
                                      /*OUT[string]*/ safearray_handle** strModuleVersionList) = 0;
    /*This function is used for getting server os type.*/
    virtual bool getOSType(/*OUT[string]*/ safearray_handle * *strOSType) = 0;
    /*This function is used for invoke a system call on server system.*/
    virtual bool remoteSystemInvoke(/*[IN]*/ const char* strSystemCommand) = 0;
    /*This function is used for change the language of server system.*/
    virtual bool setSystemLanguage(/*[IN]*/ const char* strLanguage) = 0;
    /*This function is used for get time(unit:ns) in rpc service.*/
    virtual bool getRpcTime(/*[OUT]*/ unsigned long long& ullRpcTime) = 0;
};

ENABLE_RECOGNIZE_MACRO(IFramework)
